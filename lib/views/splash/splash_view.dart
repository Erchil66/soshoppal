import 'package:flutter/material.dart';
import 'package:soshoppal/constants/colors.dart';

class SplashScreeView extends StatelessWidget {
  const SplashScreeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: whiteColor,
      body: SafeArea(
          child: Column(
        children: const [],
      )),
    );
  }
}
